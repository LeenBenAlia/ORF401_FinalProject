# Deploys the React app to GitHub Pages using the official Actions workflow.
#
# ONE-TIME SETUP (required or you will see "there isn't a GitHub Pages site"):
#   1. Repo → Settings → Pages
#   2. Under "Build and deployment" → Source: choose "GitHub Actions" (not "Deploy from a branch")
#   3. Push to main (or run this workflow manually). After the first green run, open:
#      https://LeenBenAlia.github.io/ORF401_FinalProject/
#
# If you rename the repository, update PUBLIC_URL below to /YourNewRepoName
name: Deploy frontend to GitHub Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: npm
          cache-dependency-path: frontend/package-lock.json

      - name: Install and build React app
        working-directory: frontend
        env:
          CI: false
          PUBLIC_URL: /ORF401_FinalProject
          REACT_APP_API_BASE_URL: ${{ secrets.REACT_APP_API_BASE_URL }}
        run: |
          npm ci
          npm run build

      - name: Copy index to 404 for client-side routes (SPA on GitHub Pages)
        run: cp frontend/build/index.html frontend/build/404.html

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: frontend/build

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
